version: 1
frontend:
  phases:
    build:
      commands:
        - nvm install 18
        - npm install
        - npm run build
        - echo "Building hoops app..."
  cache:
    paths:
      - 'node_modules/**/*'